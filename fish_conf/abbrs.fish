abbr -a ggrep git grep -Finw -C 5
abbr -a --position anywhere grepw grep -Finw -C 5

# For macOS `log` tool
abbr -a slf set log_file

# ls helpers
abbr -a ll ls -alF
abbr -a la ls -A
abbr -a l ls -CF

